# mpv Smart Autosub

## 简介
这是一个 mpv 播放器的 Lua 脚本，用于自动加载文件名不完全一致但包含相同“季/集”编号（如 S01E04）的字幕。

## 安装
1. 下载 `smart_autosub.lua`。
2. 将其放入 mpv 的 `scripts` 文件夹。
   - Windows: `%AppData%\mpv\scripts\`
   - Mac/Linux: `~/.config/mpv/scripts/`
